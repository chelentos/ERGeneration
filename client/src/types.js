const MUTATION_TYPE = {
  setReqs: 'setReqs',
}

const ACTION_TYPE = {
  sendReqs: 'sendReqs',
}

const GETTER_TYPE = {
  getReqs: 'getReqs',
}

export {
  MUTATION_TYPE,
  ACTION_TYPE,
  GETTER_TYPE,
}
